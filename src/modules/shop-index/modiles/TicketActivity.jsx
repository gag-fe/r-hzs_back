import React from 'react';
import ReactDOM from 'react-dom';
import { createContainer, createRootContainer } from 'Roof';

require('../style/index.less');

const TicketActivity=React.createClass({
	render(){
		return (
           <div>
             TicketActivity 
           </div>
		)
	}
});

export default TicketActivity;