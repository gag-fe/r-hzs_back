import React from 'react';
import ReactDOM from 'react-dom';
import { createContainer, createRootContainer } from 'Roof';

require('../style/index.less');

const GagActivity=React.createClass({
	render(){
		return (
           <div>
             GagActivity 
           </div>
		)
	}
});

export default GagActivity;